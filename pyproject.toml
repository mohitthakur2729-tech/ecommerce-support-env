[project]
name = "ecommerce-support-env"
version = "0.1.0"
description = "Ecommerce Support RL Agent"
dependencies = [
    "openai",
    "pydantic",
    "python-dotenv",
    "gradio",
    "openenv>=0.2.0"
]

[project.scripts]
server = "server.app:app"